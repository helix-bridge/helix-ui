name: Deploy develop

on:
  pull_request:

jobs:
  test-tag:
    name: Test tag
    runs-on: ubuntu-latest
    if: matrix.package == 'apps'
    strategy:
      matrix:
        package:
          - apps
          - helix 
    steps:
      - uses: actions/checkout@v2

      - run: echo ${{ matrix.package }}
