name: Deploy develop

on:
  pull_request:

jobs:
  test-tag:
    name: Test tag
    runs-on: ubuntu-latest
    if: ${{ contains(github.event.ref, '-${{ matrix.package }}') }}
    # if: contains(github.event.ref, '-') == false
    # if: contains(github.event.ref, matrix.package)
    strategy:
      matrix:
        package:
          - apps
          - helix 
    steps:
      - uses: actions/checkout@v2

      - name: test
        if: contains('apps', matrix.package)
        run: echo ${{ matrix.package }}
