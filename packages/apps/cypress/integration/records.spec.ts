/// <reference types="cypress" />

describe('History records', () => {
  before(() => {
    cy.activeMetamask();
  });

  it('should display ethereum to darwinia records', () => {
    cy.get('')
  });

  it('should display darwinia to ethereum records', () => {
  });
});
